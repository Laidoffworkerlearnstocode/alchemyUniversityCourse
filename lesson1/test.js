const { sha256 } = require("ethereum-cryptography/sha256")
const { toHex, utf8ToHex } = require("ethereum-cryptography/utils")

const COLORS = ["red", "green", "blue", "yellow", "pink", "orange"]

function findColor(hash) {
    
}

module.exports = findColor;